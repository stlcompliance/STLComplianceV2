name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

permissions:
  contents: read

jobs:
  dotnet:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "10.0.x"

      - name: Restore
        run: dotnet restore STLCompliance.slnx

      - name: Build
        run: dotnet build STLCompliance.slnx --configuration Release --no-restore

      - name: Test
        run: dotnet test STLCompliance.slnx --configuration Release --no-build --verbosity normal --filter "Category!=Live"

      - name: OpenAPI parity
        run: dotnet test tests/STLCompliance.OpenApi.Tests/STLCompliance.OpenApi.Tests.csproj --configuration Release --no-build --verbosity normal --filter "Category=OpenApi"

      - name: OTEL smoke checks
        run: dotnet test tests/STLCompliance.Otel.Tests/STLCompliance.Otel.Tests.csproj --configuration Release --no-build --verbosity normal --filter "Category=Otel"

      - name: DR restore drill checks
        run: dotnet test tests/STLCompliance.Dr.Tests/STLCompliance.Dr.Tests.csproj --configuration Release --no-build --verbosity normal --filter "Category=Dr&Category!=Live"

      - name: Load test harness checks
        run: dotnet test tests/STLCompliance.Load.Tests/STLCompliance.Load.Tests.csproj --configuration Release --no-build --verbosity normal --filter "Category=Load&Category!=Live"

      - name: CI frontend catalog checks
        run: dotnet test tests/STLCompliance.E2E/STLCompliance.E2E.csproj --configuration Release --no-build --verbosity normal --filter "Category=Ci&Area=Frontend"

      - name: Render blueprint catalog checks
        run: dotnet test tests/STLCompliance.E2E/STLCompliance.E2E.csproj --configuration Release --no-build --verbosity normal --filter "Category=Ci&Area=RenderBlueprint"

      - name: Render staging ship gate catalog checks
        run: dotnet test tests/STLCompliance.E2E/STLCompliance.E2E.csproj --configuration Release --no-build --verbosity normal --filter "Category=Ci&Area=RenderStagingShipGate"

      - name: EF migrations present
        run: |
          test -f apps/nexarr-api/NexArr.Api/Migrations/*InitialPlatformFoundation*.cs
          test -f apps/staffarr-api/StaffArr.Api/Migrations/*InitialPlatformFoundation*.cs
          test -f apps/compliancecore-api/ComplianceCore.Api/Migrations/*InitialPlatformFoundation*.cs
          test -f apps/assurarr-api/AssurArr.Api/Migrations/*InitialPlatformFoundation*.cs

  stlcompliancesite:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: apps/stlcompliancesite/package-lock.json

      - name: Install
        working-directory: apps/stlcompliancesite
        run: npm ci

      - name: Build
        working-directory: apps/stlcompliancesite
        run: npm run build

      - name: Test
        working-directory: apps/stlcompliancesite
        run: npm test

  stlcompliancekb:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: apps/stlcompliancekb/package-lock.json

      - name: Install
        working-directory: apps/stlcompliancekb
        run: npm ci

      - name: Build
        working-directory: apps/stlcompliancekb
        run: npm run build

      - name: Test
        working-directory: apps/stlcompliancekb
        run: npm test

  suite-frontend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: apps/suite-frontend/package-lock.json

      - name: Install
        working-directory: apps/suite-frontend
        run: npm ci

      - name: Build
        working-directory: apps/suite-frontend
        run: npm run build

      - name: Test
        working-directory: apps/suite-frontend
        run: npm test

  routarr-frontend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: apps/routarr-frontend/package-lock.json

      - name: Install
        working-directory: apps/routarr-frontend
        run: npm ci

      - name: Build
        working-directory: apps/routarr-frontend
        run: npm run build

      - name: Test
        working-directory: apps/routarr-frontend
        run: npm test

  staffarr-frontend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: apps/staffarr-frontend/package-lock.json

      - name: Install
        working-directory: apps/staffarr-frontend
        run: npm ci

      - name: Build
        working-directory: apps/staffarr-frontend
        run: npm run build

      - name: Test
        working-directory: apps/staffarr-frontend
        run: npm test

  trainarr-frontend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: apps/trainarr-frontend/package-lock.json

      - name: Install
        working-directory: apps/trainarr-frontend
        run: npm ci

      - name: Build
        working-directory: apps/trainarr-frontend
        run: npm run build

      - name: Test
        working-directory: apps/trainarr-frontend
        run: npm test

  maintainarr-frontend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: apps/maintainarr-frontend/package-lock.json

      - name: Install
        working-directory: apps/maintainarr-frontend
        run: npm ci

      - name: Build
        working-directory: apps/maintainarr-frontend
        run: npm run build

      - name: Test
        working-directory: apps/maintainarr-frontend
        run: npm test

  supplyarr-frontend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: apps/supplyarr-frontend/package-lock.json

      - name: Install
        working-directory: apps/supplyarr-frontend
        run: npm ci

      - name: Build
        working-directory: apps/supplyarr-frontend
        run: npm run build

      - name: Test
        working-directory: apps/supplyarr-frontend
        run: npm test

  compliancecore-frontend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: apps/compliancecore-frontend/package-lock.json

      - name: Install
        working-directory: apps/compliancecore-frontend
        run: npm ci

      - name: Build
        working-directory: apps/compliancecore-frontend
        run: npm run build

      - name: Test
        working-directory: apps/compliancecore-frontend
        run: npm test

  ledgarr-frontend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: apps/ledgarr-frontend/package-lock.json

      - name: Install
        working-directory: apps/ledgarr-frontend
        run: npm ci

      - name: Build
        working-directory: apps/ledgarr-frontend
        run: npm run build

      - name: Test
        working-directory: apps/ledgarr-frontend
        run: npm test

  loadarr-frontend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: apps/loadarr-frontend/package-lock.json

      - name: Install
        working-directory: apps/loadarr-frontend
        run: npm ci

      - name: Build
        working-directory: apps/loadarr-frontend
        run: npm run build

      - name: Test
        working-directory: apps/loadarr-frontend
        run: npm test

  reportarr-frontend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: apps/reportarr-frontend/package-lock.json

      - name: Install
        working-directory: apps/reportarr-frontend
        run: npm ci

      - name: Build
        working-directory: apps/reportarr-frontend
        run: npm run build

      - name: Test
        working-directory: apps/reportarr-frontend
        run: npm test

  assurarr-frontend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: apps/assurarr-frontend/package-lock.json

      - name: Install
        working-directory: apps/assurarr-frontend
        run: npm ci

      - name: Build
        working-directory: apps/assurarr-frontend
        run: npm run build

      - name: Test
        working-directory: apps/assurarr-frontend
        run: npm test

  customarr-frontend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: apps/customarr-frontend/package-lock.json

      - name: Install
        working-directory: apps/customarr-frontend
        run: npm ci

      - name: Build
        working-directory: apps/customarr-frontend
        run: npm run build

      - name: Test
        working-directory: apps/customarr-frontend
        run: npm test

  ordarr-frontend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: apps/ordarr-frontend/package-lock.json

      - name: Install
        working-directory: apps/ordarr-frontend
        run: npm ci

      - name: Build
        working-directory: apps/ordarr-frontend
        run: npm run build

      - name: Test
        working-directory: apps/ordarr-frontend
        run: npm test
